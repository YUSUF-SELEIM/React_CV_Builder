/* eslint-disable react/prop-types */
import { Button } from "flowbite-react";

export default function LoadExample({ handleDebugData }) {
  return <Button onClick={handleDebugData}>Load Example</Button>;
}
